import { addTodo, listTodos } from './todos'
import {
  getArticles,
  getArticlesByDate,
  createArticle,
  updateArticle,
  deleteArticle,
} from './articles'

export default {
  listTodos,
  addTodo,
  getArticles,
  getArticlesByDate,
  createArticle,
  updateArticle,
  deleteArticle,
}
